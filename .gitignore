# Build output (regenerated by build.mjs / CI)
dist/

# Album-art cache — downloaded from Deezer by build.mjs. Not committed: in CI
# it's persisted with actions/cache; locally it just accumulates between
# builds. Delete it any time to re-fetch.
art/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
