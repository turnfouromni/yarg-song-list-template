{
  "name": "yarg-song-list",
  "version": "1.0.0",
  "private": true,
  "description": "Static, interactive YARG song list built from songs.json",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "test:unit": "node --test test/unit.test.mjs test/build.test.mjs",
    "test:dom": "node --test test/dom.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
